version: "3.8"

services:
  funding-farmer:
    build: .
    container_name: funding-fee-farmer
    env_file:
      - .env
    volumes:
      - ./data:/app/data
      - ./logs:/app/logs
    restart: unless-stopped
    environment:
      - PYTHONUNBUFFERED=1
